alias editrc='vi ~/.bashrc'
alias sourcerc='source ~/.bashrc'

alias g="git"
alias ga="git add"

alias gb="git branch"
alias gba="git branch -a"
alias gbd="git branch -d"

alias gc="git commit"
alias gca="git commit -a"

alias gch="git checkout"

alias gl="git log --graph --oneline --decorate --pretty=format:'%Cred%h%Creset %C(yellow)%d%Creset %s %Cgreen(%cr)%Creset %Cblue[%an]%Creset' --date=relative"
alias gdl="gl --cherry-pick"

alias gs="git status"
alias gsb="git status -sb"

alias gst="git stash"

alias gf="git fetch"

alias gm="git merge"

alias gpsh="git push --tags"
alias gpsho="git push --tags origin"

alias gpl="git pull"
alias gplo="git pull origin"

alias gd="git diff --color"
alias gdpat="git diff --color --patience"
alias gds="git diff --color --stat"

alias gt="git tag"

alias gsh="git show --color"
alias gshs="git show --color -s"
alias gshst="git show --color --stat"

alias gbl="git blame --date=short"
